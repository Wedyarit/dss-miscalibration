{
  "name": "dss-miscalibration",
  "private": true,
  "scripts": {
    "dev": "npx concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend && PYTHON=$(test -x .venv/bin/python && echo .venv/bin/python || echo venv/bin/python) && \"$PYTHON\" -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
